version: '3.8'

services:
  download-manager:
    build: ./server
    ports:
      - "6199:6199"
    volumes:
      - ./downloads:/downloads
      - ./data:/app/data
    environment:
      - API_KEY=${API_KEY}
      - ALLOWED_ORIGINS=${ALLOWED_ORIGINS:-*}
      - PORT=6199
    restart: unless-stopped
